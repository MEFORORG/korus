# Handoff -- 2026-09-21, branch claude/lander-conflicts-code-review-32b7a6

Written because the session ran low on usage. Delete this file when its work is done.

## State

Two commits, both pushed. Verified with `git ls-remote --heads origin`: tip `6ee23c5`.

| Commit | What |
|---|---|
| `7dcd00a` | Owner rulings 2026-09-21: a content conflict is the Lander's; the Lander is not a second reader; the /driver rules are always on. |
| `6ee23c5` | Compression of CLAUDE.md, the template, six playbooks, roles/README.md and six role cards. |

**No pull request is open for this branch.** Opening it is the next mechanical step.

## Not done

### 1. The Opus rule, drafted but NOT applied

Owner instruction, this session's chat: *"all subagents should spawn with Opus as the model"*,
scoped to `roles/LANDER.md`. It was blocked all session because a compression worker held that
file. That worker has finished, so the file is free.

Two pieces. One row at the end of the *Standing rules that a fresh message will not override*
table, and a `####` subsection after the *Prefer a SPAWNED SESSION over a subagent when you
dispatch a repair* table.

Substance: every `Agent` dispatch passes `model: opus`; omitting it inherits a configured default
you cannot read from inside the dispatch; a `fork` subagent ignores the parameter because it always
inherits the parent model. **State the limit too:** the ruling names SUBAGENTS, not spawned
sessions, and this repository narrowed a rule on 2026-09-18 for exactly that shape of inference.

### 2. Eleven defects, all located and measured, none fixed

The compression workers found these and were told not to fix them. Analysis is complete; the edits
are not written.

| # | Site | Defect |
|---|---|---|
| 1 | `roles/COMMON.md:38` | Heading reads *"Two repositories"*; line 42 reads *"Three repositories, not two, since 2026-09-04."* |
| 2 | `roles/README.md:20` | Heading says *"nine seats have retired"*; line 68 lists ten files and says *"Ten seats, ten files."* Ten is right: the Regulator retired 2026-09-19, after that heading was written. |
| 3 | `roles/COMMON.md:503` | Doubled phrase: *"the last section of the definition's last section is load-bearing"*. |
| 4 | `roles/MANAGER.md` 196, 199, 202 | Says a Manager's workers are subagents only, and *"Needed the spawn grant: no"*. `CLAUDE.md`'s seat table GOVERNS and says *"subagents or as separate sessions"*; `roles/LANDER.md` section 2 quotes Owner ruling 2026-09-16, engine PR 1193, granting a Manager the spawn. MANAGER.md is the stale copy. Record it as AMENDED; do not delete the comparison table. |
| 5 | `docs/roles/manager.card.md:39` | *"Do not check the pool before you open a PR"* is correct and live but undated. The playbook records it at `roles/MANAGER.md:28` as RETIRED 2026-09-18 by Owner ruling. Add the attribution. |
| 6 | `roles/MANAGER.md:290` | *"This section already states that rule."* Section 2a does not. Section 2 does, under *"put your ledger row in its OWN commit, LAST"*. |
| 7 | `roles/BUILDER.md:1099` | *"Put it on the pull request."* This seat cannot post on a pull request it does not open. See the CHANGED 2026-09-18 markers at lines 155 and 967, and line 112. Route it to the report and the Manager. ONE site, not three: the other two a worker named no longer match after compression. |
| 8 | `roles/BUILDER.md:706` | Cites *"Section 4c holds the three shapes"*. 4c's own CORRECTED 2026-09-20 block at line 618 says *"at least three tags"* and the table lists six rows. Drop the count rather than restate it. |
| 9 | `roles/STEWARD.md:353` | See the measurement below. The sentence needs rewriting, not a number swap. |
| 10 | `roles/STEWARD.md` 2d | One measurement printed twice: the regime table `92% -> 96% -> 99%` and the legs `+44, +28, +17.5`. Article VI protects both halves. Probably leave it. Named here so nobody re-finds it as new. |
| 11 | `roles/WATCHDOG.md:356` | A duplication cut removed section 8's *"The seat found out when the Owner asked where the board was"*, leaving only 1b's *"The Owner found out by asking"*. Same event, opposite sides. Restore the seat side if that distinction is load-bearing. |

### Defect 9, measured so nobody has to re-derive it

`roles/STEWARD.md:353` reads:

> Over this section's 242 lines, `caller` **0** and `own reading` **0**, against controls of `rate`
> **27**, `regime` **9**, `projection` **22**.

The subject moved after the measurement. Writing section 2h put the word `caller` into section 2,
so the probe now swallows the text that answers it. Measured at `6ee23c5`, extracting each range
with `sed -n` and counting with `grep -o -i <word> | wc -l`:

| Scope | lines | caller | own reading | rate | regime | projection |
|---|---|---|---|---|---|---|
| Section 2, lines 84-388 | 305 | 8 | 2 | 46 | 8 | 21 |
| Section 2 EXCLUDING 2h, lines 84-344 | 261 | **0** | 1 | 41 | 7 | 20 |
| Section 2h alone, lines 345-388 | 44 | 8 | 1 | 5 | 1 | 1 |

**The finding's shape survives in the narrow scope and its numbers do not.** `caller` is still 0
outside 2h, so the original absence holds where it was taken. `own reading` is now 1 there, at line
333, so that half no longer reproduces. Every control moved, and 242 matches no current range.

Honest repair: state the scope as section 2 excluding 2h, keep the original figures marked as of
their date, and publish this re-measurement beside them with its command. Do not swap 242 for 261
and leave the rest standing.

## Warnings for whoever picks this up

**Do not rewrap the `CLAUDE.md` paragraph beginning "Push your own branch and open your own PR,
without asking."** `tests/test_the_seat_that_opens_a_pr_is_stated_once.py` fires when "open ...
your own PR" and a universal such as "every seat" land on the SAME line. They sit on separate
lines, and any reflow that joins them turns that test red.

**`CLAUDE.md` carries two single lines of about 285 and 275 characters** in the Watchdog
paragraphs, against a 100-column file. Wrapping them correctly ADDS four lines. Left for a ruling.

**Open PR #143 edits `roles/LANDER.md` and `docs/roles/lander.card.md`**, from another session's
worktree at `.claude/worktrees/fleet-mail-process-docs-cf242f`. That session went ARCHIVED and
could not be messaged, so it does not know this branch exists. Four regions of LANDER.md are
byte-identical to HEAD so #143 still merges. The card is 145 lines and 7,779 bytes against caps of
150 and 8,192, so #143's roughly 300 bytes fits.

**Never run the full test suite while subagents are writing the tree.** It takes 19 to 46 minutes,
and a worker holding a file mid-write produces a `PermissionError` that reads exactly like a real
failure. Two runs were wasted that way.

**A piped pytest run reports the pipe's exit code, not pytest's.** Both background runs notified
"exit code 0" while one of them had a failure. Read the output file, not the notification.

**One worker reported that pytest here gets an invalid stdin handle, so every git-shelling test
reports a phantom red.** Measured in the main session's own shell: `test_docs_do_not_drift` and
`test_expiry_audit` returned 49 passed, 321 subtests, zero failures, and the 46-minute full run
returned 740 passed with none. That claim is true of the worker's shell and false of this one.
